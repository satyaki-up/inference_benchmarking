[project]
name = "inference-benchmarking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "datasets==3.6.0",
    "torch>=2.9.1",
    "transformers",
]
